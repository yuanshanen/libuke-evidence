server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /api
  undertow:
    threads:
      io: 4
      worker: 32

spring:
  application:
    name: libuke-evidence-api
  profiles:
    active: local
